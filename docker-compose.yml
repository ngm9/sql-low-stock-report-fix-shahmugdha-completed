services:
  db:
    image: postgres
    environment:
      POSTGRES_DB: marketplace
      POSTGRES_USER: utkrusht
      POSTGRES_PASSWORD: utkrusht
    ports:
      - "5432:5432"
    volumes:
      - ./init_database.sql:/docker-entrypoint-initdb.d/init_database.sql
